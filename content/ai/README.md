# AI Lab 笔记

这里放 AI Lab 内容：文章、前端笔记、AI 工作流记录和工程写作。

推荐流程：

1. 复制 `_template.md`。
2. 改成日期开头的文件名，例如 `2026-07-08-first-note.md`。
3. 用 Markdown 写正文。
4. 如果需要图片或附件，放到 `content/ai/assets/`。
5. 准备发布时，把 `status` 改成 `published`。

建议文件名格式：

```text
YYYY-MM-DD-ai-slug.md
```
